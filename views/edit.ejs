<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h2>Edit your chat</h2>
    <h5>Ur chat id: <%= chat._id %>/h5>
    <p>from <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form action="/chats/<%=chat._id%>?_method=PUT" method="POST">
        <textarea name="msg"> <%= chat.msg %></textarea><br>
        <button>Edit</button>
    </form>
</body>
</html>